{
  "name": "rate-limit-promise-pool",
  "version": "1.0.0",
  "description": "A class to run a group of promises while keeping the number running at one time and the minimum time between each one consistent",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^4.0.3"
  },
  "files": [
    "dist"
  ]
}
